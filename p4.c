/* Write a program to find the additioon of two numbers. */

#include<stdio.h>
int main()
{
    int a,b,c;
    printf("Enter two numbers: ");
    scanf("%d%d",&a,&b);
    c=a+b;
    printf("The sum is: %d",c);
    return 0;
}